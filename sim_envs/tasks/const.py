"""

"""
from pathlib import Path
from sim_envs.const import DATA_DIR_PATH

TASK_CONFIGS = {
    "NeedlePick-v0": {
        'dataset_dir': Path(DATA_DIR_PATH) / 'demo' / 'NeedlePick-v0-100',
        'num_episodes': 80,
        'episode_len': 600,
        'camera_names': ['env_cam', 'wrist_cam']
    },
}